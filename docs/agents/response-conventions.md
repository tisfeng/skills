# 回复与交付约定

- 先说明结果，再说明必要证据、未验证项或下一步。
- 区分已执行、未执行、阻塞和建议；不得把计划写成已完成。
- 使用 planner 后先交付方案，等待明确执行授权。
- implementation 在仓库规则授权且门禁通过时默认自动本地提交；提交回执遵循 `git-commit`
  Skill 的完整格式。跳过自动提交时说明具体门禁原因。
- push、pull、rebase、merge、发布和改变外部服务仍只在用户或对应工作流明确授权时执行。
- 引用仓库文件时使用相对路径；引用用户本机文件时使用完整路径。
