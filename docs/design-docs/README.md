# 设计文档

本目录保存需要长期维护的重要设计决策。当前 Agent 执行规则以根 [`AGENTS.md`](../../AGENTS.md)
和 `docs/agents/` 为权威；任务过程与完成结果分别位于 `docs/exec-plans/` 和
`docs/histories/`。

设计文档保留状态、日期、背景、取舍和重新评估条件，并随相关实现更新。本目录不保存任务日志，
也不作为第二套 Agent 任务路由。
