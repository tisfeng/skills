# 发布 Skills v0.3.0

- 状态：active
- 创建日期：2026-09-08
- 负责人：Codex
- 关联 Issue/PR：none

## 任务摘要

将自 `v0.2.0` 以来的通用 Agent 交付修复、README 改进和 Electron/TypeScript Skill
能力作为 `v0.3.0` 统一发布，并将 `@tisfeng/codex-agents` 的 npm 版本与 Git tag 对齐。

## 授权与范围

- 意图模式：implementation
- 交付授权：release，包含本地提交、push `main`、创建并 push `v0.3.0` tag、
  npm publish 和 GitHub Release。
- 安全状态：normal；版本占用、远程漂移、验证或发布失败时转为 protected。
- 仓库允许路径：`package.json`、本计划的 active/completed 路径和
  `docs/histories/2026-09/2026-09-08-publish-v0.3.0.md`。
- 持续限制：不修改用户真实 Codex 全局配置；安装验证只使用临时目录。

## 初始 Git 快照

- `initial_head`：`d94c9bfeabf370a04b213000f34997b2b8035848`
- 分支：`main`
- 初始 staged：none
- 初始 unstaged：none
- 初始 untracked：none
- 初始冲突：none
- 远程 `main`：`8cc761cfdb51c53d532ef9e5df99e0d0118db760`
- 远程 `v0.3.0`：not found
- npm `latest`：`0.2.0`；`0.3.0` not found

## 执行步骤

1. 将 package 版本更新为 `0.3.0`，记录发布计划与 history。
2. 运行与 CI 对齐的 Skill、Agent、Python、Node、Shell 和 npm tarball 验证。
3. 创建本地发布提交，push `main` 并等待 CI 通过。
4. 创建并 push annotated tag `v0.3.0`，等待 tag CI 通过。
5. 发布 `@tisfeng/codex-agents@0.3.0`，然后从既有 tag 创建 GitHub Release。
6. 在临时项目验证固定版本安装、lock 记录和无变更 update。
7. 将本计划移至 completed，更新 history 并 push 最终记录。

## 完成条件

- `main` 和 `v0.3.0` 的 CI 通过。
- npm `latest` 为 `0.3.0`，Git tag、GitHub Release 与发布提交一致。
- 临时安装验证通过，未修改用户真实 Codex 配置。
- 最终 `main` 工作树干净，发布证据记录完整。
