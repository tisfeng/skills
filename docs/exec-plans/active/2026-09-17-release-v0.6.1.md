# 发布 Skills v0.6.1

- 状态：active
- 创建日期：2026-09-17
- 负责人：Codex
- 关联 Issue/PR：none

## 执行上下文

- **Agent Name:** `Codex`
- **Model ID:** `Unknown`
- **Environment:** `macOS 27.0 / Xcode 27.0 (27A266a)`

## 背景

`v0.6.0` 之后，仓库改进了 Agent 文档与执行记录模板，修正了 Worktree UI 展示名称，并让
`review-pr` 可以安全复用当前用户已有 PR 的同名本地分支。用户明确要求发布 `0.6.1`；候选没有
声明破坏性变化，因此按补丁版本 `v0.6.1` 执行。

## 目标与范围

- 目标结果：发布 `v0.6.1`，使发布提交、annotated tag、GitHub Release、固定 tag 隔离安装和发布记录可核验一致。
- 允许修改路径：本计划 active/completed 路径与
  `docs/histories/2026-09/2026-09-17-release-v0.6.1.md`。
- 同任务 history：`docs/histories/2026-09/2026-09-17-release-v0.6.1.md`。
- 用户限制：版本固定为 `0.6.1`；不修改全局配置；隔离验收仅使用临时目录；不移动或覆盖公开 tag；
  不手写 Release 正文；不发布 npm 包。
- 非目标：不改动既有候选功能或 release workflow；若发现阻塞缺陷，停止 tag 阶段并先修复、重新验证。
- 验收标准：发布提交普通 CI 通过；tag peeled SHA、Release 标题和自动生成正文一致；固定 tag 安装只包含
  6 个公开 Skills；发布记录已推送且不移动 tag。

## 工作计划

1. 冻结 `v0.6.0..HEAD` 候选，复验远端基线、认证、tag/Release 占用、完整审查与验证矩阵。
2. 创建 Angular-style 发布提交，复验 `origin/main` 未漂移后 push，等待该 SHA 的普通 CI 成功。
3. 再次核验 `v0.6.1` 占用状态，创建并 push 指向发布提交的 annotated tag。
4. 等待 tag workflow 创建 GitHub Release，核验 title、默认 Release Notes、tag peeled SHA 和 workflow。
5. 在临时消费者从固定 `tisfeng/skills#v0.6.1` 安装，回填证据、归档计划并提交/push 发布记录。

## 风险与决策

- 本地 `main` 初始为 `2659be272cc186812710744c77cd9f9e7ba7c709`，远端 `main` 为
  `cb77b204f5d016b7c963053a04be33f2031b9d82`；远端是本地祖先，本地领先 3 个提交。
- 用户明确指定 `v0.6.1`；候选没有声明 `BREAKING CHANGE`，不擅自调整目标版本。
- 普通 CI 失败时不创建 tag；公开 tag 校验失败且需要修改源码时使用下一版本；Release 创建失败只恢复 Release 阶段。
- 候选及固定 tag 安装使用临时 npm cache，避免修改用户全局 npm cache。

## 进度

- [x] 初始只读预检完成，目标版本、远端基线和占用状态明确。
- [x] 候选审查、完整本地验证与临时消费者安装通过。
- [ ] 发布提交已推送，普通 CI 通过。
- [ ] annotated tag、tag workflow 与 GitHub Release 已核验。
- [ ] 固定 tag 隔离安装与发布记录已完成。

## 验证

- 初始快照：`v0.6.0..2659be272cc186812710744c77cd9f9e7ba7c709`；工作树和索引干净。
- 远端 `main` 为 `cb77b204f5d016b7c963053a04be33f2031b9d82`，是本地 HEAD 的祖先；本地领先 3 个提交。
- `v0.6.1` 本地/远端 tag 与 GitHub Release 未占用；GitHub 认证及普通 CI、Release workflow 可用。
- `review` 冻结范围为 `v0.6.0..2659be272cc186812710744c77cd9f9e7ba7c709`，fingerprint 为
  `797df67f1664cb5e7050b4f31017c094f2a87141a1eb4b2968cc3fb958a65081`，patch SHA-256 为
  `421e9d201c4ebf98efd45253b0f79b23a8b84b82607182e7f86077637f3e2448`；复核未发现有效 finding，
  交付前漂移检查为 `unchanged`。
- `validate-skills.py`、`compileall`、`bash -n`、`git diff --check` 与 6 组现有测试均通过，共 112 项测试。
- 从 `git archive HEAD` 生成无污染候选源码，在临时消费者中用 `skills@1.5.25` 复制安装 6 个公开
  Skills；安装树逐一等于源码，独立重算哈希逐一等于 `skills-lock.json`，且没有暴露内部 `release` Skill。
- CI、tag、Release 与固定 tag 安装证据将在对应阶段回填。

## 完成条件

- [ ] 发布提交已在远程 `main` 且普通 CI 通过。
- [ ] `v0.6.1` tag peeled SHA、GitHub Release 标题与默认正文一致。
- [ ] 固定 tag Skills 隔离安装通过，且只包含公开 Skills。
- [ ] 计划与 history 已回填真实证据并推送，公开 tag 保持在发布提交。
