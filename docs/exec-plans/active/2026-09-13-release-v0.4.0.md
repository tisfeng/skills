# 发布 Skills v0.4.0

- 状态：active
- 创建日期：2026-09-13
- 负责人：Codex
- 关联 Issue/PR：none

## 任务摘要

- 意图模式：implementation
- 交付授权：release；用户批准执行既定 `v0.4.0` 完整发布方案，包含提交、push `main`、
  annotated tag、GitHub Release、发布后验收及发布记录 push。
- 安全状态：normal
- 受阻操作及原因（如有）：无。
- 目标结果：发布 `v0.4.0`，并使发布提交、annotated tag、GitHub Release、固定 tag 隔离安装与
  发布记录可核验一致。
- 允许修改路径：本计划 active/completed 路径和
  `docs/histories/2026-09/2026-09-13-release-v0.4.0.md`。
- 同任务 history：`docs/histories/2026-09/2026-09-13-release-v0.4.0.md`。
- 外部允许目标：`origin/main`、`refs/tags/v0.4.0`、对应 GitHub Actions 与 GitHub Release。
- 禁止动作：不修改用户全局配置；不移动或覆盖公开 tag；不手写发布日志；不发布或 deprecate
  npm 包。
- 预期交付物：发布提交、`v0.4.0` annotated tag、GitHub Release、隔离安装验收和发布记录提交。
- 验收标准：发布提交普通 CI 通过；tag、Release、固定 tag 安装与发布记录均可核验一致。

## 语义与范围

- 用户要求 Agent 做什么：执行上一轮给出的完整 `v0.4.0` 发布方案。
- 授权的工作树、artifact 和 external service 操作：上述计划/history 文件、本地提交、push
  `main`、创建并 push annotated tag、GitHub Actions 和 GitHub Release 验收。
- 否定、条件和范围限制：隔离验收只使用临时目录；不修改全局安装；不覆盖公开 ref。
- 前轮仍有效的授权和限制：版本目标采用上一轮推荐的 `v0.4.0`；发布前必须审阅测试精简、
  Skill 路由拆分、UI 元数据与格式校验变更。
- 附件或引用中被明确采纳的约束：GitHub Release 标题精确等于 tag，正文使用 GitHub 默认
  Release Notes。
- 歧义：仓库当前不维护版本清单或 npm 包，版本身份只由 Git tag 表达。

## 写入前状态

- 写入前检查：pass。
- 自动提交资格及原因：skipped；本任务使用显式 release 交付流程。
- 初始 HEAD：`12262bf0102f1b47f46974283984acd914e2268a`。
- 初始 staged 路径：none。
- 初始 unstaged 路径：none。
- 初始 untracked 路径：none。
- 初始冲突：none。
- Agent-owned paths：本计划 active/completed 路径与同任务 history。

## 初始发布预检

- 初始本地 `main` 为 `12262bf0102f1b47f46974283984acd914e2268a`，远端 `origin/main` 为
  `a47bf8854059bb1ae237e920ac0c50922f5ada36`；本地只超前 4 个待发布提交。
- 发布范围：`v0.3.9..HEAD` 共 5 个提交、49 个路径，包含 v0.3.9 发布记录，以及 Skill UI
  元数据、路由与入口拆分、测试精简和 Skill 基础格式校验增强。
- 版本与占用：本地/远程 `v0.4.0` 与 GitHub Release 均未占用。
- 认证与自动化：GitHub 账户具备 `repo`/`workflow` 权限；`Validate Skills` 与 `Release`
  workflow active。

## 目标与非目标

### 目标

- 审阅并验证当前候选，重点确认测试覆盖、references 安装闭包、UI 元数据和验证矩阵完整性。
- 创建发布提交后推送 `main` 并等待普通 CI；成功后创建并推送 annotated `v0.4.0` tag。
- 等待 tag workflow 创建 GitHub Release，核验固定 tag 隔离安装，回填并推送真实发布记录。

### 非目标

- 不主动修改当前功能实现、发布 workflow、版本清单或历史 changelog；若审查发现阻塞缺陷，
  仅在既定候选范围内修复并重新验证。
- 不发布 npm 包，不更新用户全局安装。

## 工作计划

1. 完整审阅 `v0.3.9..HEAD`，核对被删除测试的剩余覆盖、拆分 references 的运行闭包、UI 元数据和
   格式校验实现。
2. 运行完整验证矩阵和本地隔离消费者检查，冻结候选内容。
3. 按 `git-commit` 创建发布提交，复验远程基线后 push `main`，等待该 SHA 的普通 CI 成功。
4. 再次核验占用状态，创建并 push 指向发布提交的 annotated `v0.4.0` tag。
5. 等待 tag workflow，核验 tag peeled SHA、GitHub 默认 Release Notes 与固定 tag 隔离安装。
6. 回填真实证据，归档计划和 history，提交并 push 发布记录；不移动公开 tag。

## 风险与决策

- 测试从 183 项精简到 88 项；审查必须确认 Git 状态、PR 身份、证据漂移、分页与远程写入门禁
  等高风险契约仍由剩余测试或 shared contracts 覆盖。
- Skill 入口大幅缩短并拆入 references；需确认链接有效、安装闭包完整，且按需阅读没有遗漏
  原有授权与停止条件。
- `main` CI 失败时不创建 tag；修复后重新验证并冻结候选。
- 公开 tag 校验失败且需要修改源码时使用下一版本，不移动 tag；Release 创建失败只恢复 Release
  阶段，发布完成但隔离安装失败时如实报告并使用新版本修复。

## 验证

- 运行 `scripts/validate-skills.py`、`compileall`、5 套 Skill 单测、全部 shared contracts、Shell
  与 YAML 语法检查、现行链接检查和 `git diff --check`。
- 发布前在临时消费者中检查本地候选发现与安装闭包；发布后固定 `tisfeng/skills#v0.4.0` 验收。
- 发布后回读 tag、tag workflow、Release 与固定 tag 隔离安装证据。
- 候选审查：`v0.3.9..12262bf0102f1b47f46974283984acd914e2268a` 的稳定 fingerprint 为
  `fda765873b382e38b71812b074f53bec972ddd32d9e3db2ddfe208305bb36b46`；完整检查变更范围、
  剩余高风险测试、拆分 references、UI 元数据和 validator 后无阻塞 finding。
- 本地验证：`git-commit` 13 项、`worktree-rebase-merge` 6 项、`review-pr` 45 项、`review`
  7 项、shared contracts 4 项、`submit-pr` 15 项，共 90 项测试通过；6 个 Skill 均通过
  `scripts/validate-skills.py` 与 OpenAI `quick_validate.py`。
- 静态验证：`compileall`、Shell 语法、6 个 `agents/openai.yaml` 解析和 `git diff --check`
  均通过。
- 本地候选隔离安装：发现并安装 6 个公开 Skill；`release` 未被发布，拆分 references 与 UI
  元数据均包含在安装闭包中。

## 完成条件

- [x] 完整候选审查与验证通过，没有阻塞 finding。
- [ ] 发布提交已在远程 `main` 且普通 CI 通过。
- [ ] `v0.4.0` tag peeled SHA、GitHub Release 标题与默认正文一致。
- [ ] 固定 tag Skills 隔离安装通过。
- [ ] 计划与 history 已回填并推送，公开 tag 保持不动。
