# 修复 review-pr 手动回退的快照一致性

- 状态：completed；协议修订与验证完成。
- 授权：用户确认执行修复方案；仅本地实施与自动提交。
- 初始快照：`83c25a46910bbd8488ead1b46362f3e0805659f0`，detached HEAD，索引和工作树干净。
- 范围：`skills/review-pr/SKILL.md`、本计划和同任务 history；这些路径的新增修改归本任务所有。
- 限制：保留正常 helper 路径、完整分页与默认不等待 CI；不修改远程状态或全局安装。

## 工作计划

1. 集中定义手动快照协议，初始采集、独立 checks 诊断和最终刷新引用同一规则。
2. 区分 helper 不可用与已检测到漂移，明确 head 复验失败和持续漂移的退出条件。
3. 静态推演稳定 head、采集中 push、最终刷新 push 和查询失败；运行 Skill 校验及相关单测。
4. 完成 history、冻结交付范围并按 git-commit 技能本地提交。

## 风险与完成条件

- 风险：分散命令遗漏最后的 head 查询，或误把 CI 状态退出码当作读取失败。
- 完成条件：所有回退入口复用同一协议，未通过复验的 checks 不可用于 CI 复用，验证通过。
- 静态推演不等同于真实 Agent 或 GitHub 并发场景运行验证。

## 已完成验证

- 35 项 review-pr 单测、仓库 Skill 校验与 Skill Creator 快速校验通过。
- 四种文档场景推演完成；没有为文档措辞添加匹配测试。
