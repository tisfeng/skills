# 发布 Skills v0.3.8

- 状态：completed
- 创建日期：2026-09-11
- 负责人：Codex
- 关联 Issue/PR：none

## 任务摘要

发布 `v0.3.8`，使发布提交、`v0.3.8` tag、GitHub Release 与发布记录保持可核验一致。

## 授权与范围

- 用户明确发布 `0.3.8`，包含提交、push `main`、annotated tag、GitHub Actions 的 Release 创建、
  GitHub Release、发布后验收和发布记录 push。
- 仓库修改路径：本计划 active/completed 路径与
  `docs/histories/2026-09/2026-09-11-publish-v0.3.8.md`。
- 外部目标：`origin/main`、`refs/tags/v0.3.8`、对应 GitHub Actions 和 GitHub Release。
- 持续限制：不修改全局配置；隔离验收仅使用临时目录；不移动或覆盖公开 tag；不手写发布日志；
  不发布或 deprecate npm 包。

## 初始发布预检

- 初始 HEAD、`main`、`origin/main`：`98f8e18a988fd537c4670838975ae27c6b154dce`，三者一致；
  staged、unstaged、untracked、冲突：none。
- 发布范围：`v0.3.7..HEAD` 共 5 个提交、18 个路径，包含内部发布技能移出技能发现路径、README
  文案收紧、发布与 Git 交付文档合并重命名，以及校验器对 `SKILL.md` 位置和发现路径的收紧。
- 版本与占用：仓库不维护版本清单文件，版本身份由 tag 表达；本地/远程 `v0.3.8` 与 GitHub Release
  均未占用。
- 认证与自动化：GitHub 账户具备 push/admin/repo/workflow 权限；`Validate Skills` 与 `Release`
  workflow active；当前 HEAD 普通 CI 已通过。

## 发布内容

- 仓库专属发布技能不再出现在技能发现路径，也不再以 `SKILL.md` 形式分发；发布授权与流程集中到
  `docs/release/workflow.md`，公开安装只包含 6 个自我完备的 Skills。
- 发布与 Git 交付文档合并重命名：`docs/release/overview.md` → `workflow.md`，
  `docs/agents/git-workflow.md` → `git-delivery.md`，根路由与相对链接同步更新。
- `scripts/validate-skills.py` 新增 `SKILL.md` 位置校验与发现路径纯净性校验，README 文案收紧，
  公开技能源码与运行时行为未改动。

## 真实发布证据

- 候选验证：`git diff --check`、6 个 Skills 与发现入口校验、Python compileall、19 项 git-commit、
  9 项 worktree-rebase-merge、99 项 review-pr、10 项 review、38 项 submit-pr、3 项隔离消费者测试
  与 Shell 语法检查全部通过。
- 发布提交：`d79827eebdb94a7d71240f9b5a07cc0a2260c395`；普通 CI
  [`34609238370`](https://github.com/tisfeng/skills/actions/runs/34609238370) 通过。
- annotated tag `v0.3.8`：tag object `857b655a32f5ca3fa8cbc395b081b0857b463d92`，peeled commit 为
  发布提交；tag workflow [`34609347839`](https://github.com/tisfeng/skills/actions/runs/34609347839)
  的 validate 与 Create GitHub Release 两个 job 均通过，tag push 触发的普通 CI
  [`34609347561`](https://github.com/tisfeng/skills/actions/runs/34609347561) 也通过。
- GitHub Release：[`v0.3.8`](https://github.com/tisfeng/skills/releases/tag/v0.3.8) 已发布，非 draft、
  非 prerelease、无 assets；标题精确等于 tag，正文与 GitHub Release Notes API 生成的
  `v0.3.7...v0.3.8` Full Changelog 一致。
- 隔离安装：固定 `tisfeng/skills#v0.3.8` 在临时目录安装 6 个公开 Skills，全部 lock ref 为
  `v0.3.8`，仓库专属发布技能未再进入消费方安装结果。

## 完成条件

- [x] 发布提交已在远程 `main` 且普通 CI 通过。
- [x] `v0.3.8` tag peeled SHA、GitHub Release 标题与默认正文一致。
- [x] 固定 tag Skills 隔离安装通过，且只包含公开技能。
- [x] 计划与 history 已回填真实证据并作为发布记录提交 push，公开 tag 保持在发布提交。
