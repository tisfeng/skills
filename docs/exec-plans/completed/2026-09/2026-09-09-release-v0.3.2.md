# 发布 Skills v0.3.2

- 状态：completed
- 创建日期：2026-09-09
- 负责人：Codex
- 关联 Issue/PR：none

## 任务摘要

将 `v0.3.1` 之后已在本地 `main` 的用户可见交付与安装文档改进作为
`@tisfeng/codex-agents@0.3.2` 发布，并保持 npm package、Git tag、GitHub Release 与发布提交一致。

## 授权与范围

- 意图模式：implementation
- 交付授权：release，包含版本与日志修改、本地提交、push `main`、创建并 push `v0.3.2` annotated tag、
  GitHub Actions Trusted Publishing、GitHub Release、发布后验收和发布记录 push。
- 仓库允许路径：`package.json`、`docs/release/changelog/0.3.2.md`、本计划的 active/completed 路径和
  `docs/histories/2026-09/2026-09-09-publish-v0.3.2.md`。
- 外部允许目标：`origin/main`、`refs/tags/v0.3.2`、`@tisfeng/codex-agents@0.3.2`、对应 GitHub Actions
  与 GitHub Release。
- 持续限制：不修改用户真实 Codex 全局配置；安装验收只使用临时目录；不移动已公开 tag。

## 初始 Git 与发布预检

- `initial_head`：`b8d7b99730b0f48a4434ed1b5306c527a6b22302`
- 分支：`main`
- 初始 staged、unstaged、untracked、冲突：none
- 远程 `main`：`0613ad47004cb30918bd03c5e531f57a6c7d67f1`；本地仅领先 5 个提交，无分叉。
- 发布范围：`v0.3.1..HEAD`，9 个提交、40 个文件。
- 版本、远程 tag、npm 精确版本和 GitHub Release：当前 `0.3.1`；目标 `v0.3.2` 均明确未占用；npm
  `latest=0.3.1`。
- 认证与自动化：GitHub 账号可访问仓库并拥有 admin/push 权限；`npm-production` 环境和两条 workflow
  均为 active。OIDC 发布资格由本次 tag workflow 的成功结果证明。

## 发布内容

- 修复 `worktree-rebase-merge` 的空索引显式交付，并统一暂存策略。
- 强化 `git-delivery` 对冻结范围与 patch 的核验，避免重复或扩大暂存。
- 改进中英文安装文档、源码链接和 npm repository 元数据。

## 真实发布证据

- 发布前本地验证通过：6 个公开 Skills、4 个 Agents、Python compileall、6 项 Agent installer、19 项
  git-commit、2 项 worktree、27 项 review-pr、23 项 submit-pr 测试、Shell 语法与 `git diff --check`。
- `npm pack --dry-run --json`：6 个文件，候选 shasum
  `b40a92ae6b2bd72bfa8b6b5d9a501ae0fe55cb72`，integrity
  `sha512-fGlWAtIWEmdvc0R4+RyypPquKHAmXxYv3pOvSYng+VtrzXtr+Npsvsgp6p6WfWudvMNBC3LppDB7RfLdvbxBTw==`。
- 发布提交：`826b0a868f5802da8a4f2b90c8ac92e6cb2577b2`；普通 CI
  [`34333131646`](https://github.com/tisfeng/skills/actions/runs/34333131646) 通过。
- annotated tag `v0.3.2`：tag object `2bb13396b8d4c96881ae7705958bcb18ee0bbdad`，peeled commit
  为发布提交；tag workflow [`34333263811`](https://github.com/tisfeng/skills/actions/runs/34333263811)
  的验证、OIDC npm publish 与 GitHub Release 三个 job 均通过。
- npm registry：`@tisfeng/codex-agents@0.3.2` 已发布且 `latest=0.3.2`；6 个文件、shasum、integrity
  与候选完全一致，附有 SLSA provenance 与 registry 签名。隔离安装后
  `npm audit signatures --json` 返回 `invalid=[]`、`missing=[]`。
- GitHub Release：[`Skills v0.3.2`](https://github.com/tisfeng/skills/releases/tag/v0.3.2) 已发布，非 draft、
  非 prerelease，正文与 `docs/release/changelog/0.3.2.md` 一致。
- 隔离安装：固定 `tisfeng/skills#v0.3.2` 安装 7 个可发现的 Skills（包括项目内 `release`），
  `skills-lock.json` 均记录 `v0.3.2`；精确 npm `0.3.2` 安装 4 个 Agents，Agent lock 的 revision
  均为发布提交，重复 update 全部返回 `Already current`。

## 完成条件

- [x] 发布提交已在远程 `main` 且普通 CI 通过。
- [x] `v0.3.2` tag、npm `0.3.2`、`latest` 和 GitHub Release 一致。
- [x] 注册表摘要/签名与候选一致，固定 tag Skills 与精确 npm Agents 隔离安装通过。
- [x] 计划与 history 回填真实证据并作为发布记录提交 push；公开 tag 保持在发布提交。
