# 发布 Skills v0.6.2

- 状态：completed
- 创建日期：2026-09-19
- 负责人：Codex
- 关联 Issue/PR：none

## 执行上下文

- **Agent Name:** `Codex`
- **Model:** `Unknown`
- **Environment:** `macOS 27.0 / Xcode 27.0 (27A266a)`

## 背景

`v0.6.1` 之后，候选包含 `review-pr` 多阶段分支交接修复、Agent 文档治理更新，以及将仓库维护用
发布流程从 `docs/release/` 移至根目录 `release/`。用户授权继续执行新版本发布。

## 目标与范围

- 目标结果：发布 `v0.6.2`，使发布提交、annotated tag、GitHub Release、固定 tag 隔离安装和发布记录可核验一致。
- 允许修改路径：本计划 active/completed 路径、发布提交所需的发布 history，以及候选范围内既有发布资产。
- 同任务 history：`docs/histories/2026-09/2026-09-19-release-v0.6.2.md`
- 用户限制：不新增手写 Release 正文；不移动或覆盖公开 tag；不发布 npm；固定 tag 验收仅使用临时目录。
- 非目标：不修改候选功能或发布 workflow；若发布门禁失败，停止在对应阶段并按恢复规则处理。
- 验收标准：发布提交普通 CI 通过；`v0.6.2` tag peeled SHA、GitHub Release 标题和自动生成正文一致；固定 tag 安装只包含公开 Skills；发布记录已推送。

## 工作计划

1. 冻结 `v0.6.1..HEAD` 候选，完成完整 review 和验证矩阵。
2. 创建发布提交，复验远端基线后 push `main`，等待该 SHA 的普通 CI 成功。
3. 再次核验 `v0.6.2` 未占用，创建并 push annotated tag。
4. 等待 tag workflow 创建 GitHub Release，核验 tag、Release 和 workflow 证据。
5. 从固定 tag 做临时消费者安装验收，回填 history、归档计划并 push 发布记录。

## 风险与决策

- 当前 `main` 比 `origin/main` 超前 2 个提交；发布提交必须建立在当前 `7156085` 上，并在 push 前复验远端没有漂移。
- 候选没有声明破坏性变化，按补丁版本 `v0.6.2` 发布。
- 普通 CI 失败时不创建 tag；公开 tag 校验失败且需要修改代码时使用下一版本；Release 创建失败只恢复 Release 阶段。
- 根目录 `release/` 是当前发布规则入口，历史 `release/changelog/` 仅保留归档文件，新版本不新增 changelog。

## 进度

- [x] 完成初始只读预检并确认目标版本、候选端点、远端基线和占用状态。
- [x] 完成候选 review 和完整验证。
- [x] 创建发布提交并推送 `main`，等待普通 CI。
- [x] 创建 annotated tag，等待并验收 Release workflow。
- [x] 完成固定 tag 安装验收、回填 history、归档计划并推送发布记录。

## 验证

- 初始快照：`v0.6.1..7156085dabdfcf03cb0be5e3db28851e9321242c`；工作树和索引干净。
- 初始远端 `main`：`65f0d49e86dc94105c7a86e83e020acd50526bc3`；本地领先 2 个提交。
- `v0.6.2` 远端 tag 与 GitHub Release 均未占用；GitHub 认证可用。
- 已通过 `python3.12 scripts/validate-skills.py`、`python3.12 -m compileall -q scripts skills`、Shell 语法检查和 `git diff --check`。
- 完整验证矩阵共 116 项测试通过；最终 review 未发现有效 finding。
- 发布提交 `ee30f149f523a76b14df55884fe149a549798d2f` 已推送，普通 CI [`35453744373`](https://github.com/tisfeng/skills/actions/runs/35453744373) 成功。
- annotated tag `v0.6.2` 的 tag object 为 `b30ebbc0599fbf29bb563052b119de5967bd10a8`，peeled commit 为发布提交。
- tag 验证 [`35453867100`](https://github.com/tisfeng/skills/actions/runs/35453867100) 和 Release workflow [`35453867658`](https://github.com/tisfeng/skills/actions/runs/35453867658) 成功。
- GitHub Release [`v0.6.2`](https://github.com/tisfeng/skills/releases/tag/v0.6.2) 非 draft、非 prerelease，标题精确为 tag，正文为 GitHub 自动生成的 `v0.6.1...v0.6.2` Full Changelog。
- 固定 tag 临时消费者安装 6 个公开 Skills，目录级哈希和文件内容均与 `v0.6.2` tag 一致，内部 `release` Skill 未安装。

## 完成条件

- [x] 发布提交已在远程 `main` 且普通 CI 通过。
- [x] `v0.6.2` tag peeled SHA、GitHub Release 标题与默认正文一致。
- [x] 固定 tag Skills 隔离安装通过，且只包含公开 Skills。
- [x] 计划与 history 已回填真实证据并推送；公开 tag 保持在发布提交。
