# 发布 Skills v0.3.6

- 状态：completed
- 创建日期：2026-09-11
- 负责人：Codex
- 关联 Issue/PR：none

## 任务摘要

发布 `@tisfeng/codex-agents@0.3.6`，使发布提交、`v0.3.6` tag、npm、GitHub Release 与发布记录保持可核验一致。

## 授权与范围

- 用户明确发布 `0.3.6`，包含版本修改、提交、push `main`、annotated tag、GitHub Actions Trusted Publishing、
  GitHub Release、发布后验收和发布记录 push。
- 仓库修改路径：`package.json`、本计划 active/completed 路径与
  `docs/histories/2026-09/2026-09-11-publish-v0.3.6.md`。
- 外部目标：`origin/main`、`refs/tags/v0.3.6`、`@tisfeng/codex-agents@0.3.6`、对应 GitHub Actions 和
  GitHub Release。
- 持续限制：未修改全局配置；隔离验收仅使用临时目录；未移动或覆盖公开 tag；未手写发布日志。

## 初始发布预检

- 初始 HEAD、`main`、`origin/main`：`4eee13ef4814857d914f0e48940771fecd4680e0`；工作树和索引干净。
- 发布范围：`v0.3.5..HEAD` 共 4 个提交、27 个路径，包含快照校验与宿主职责解耦、Git Skill 组合与
  分支命名、PR 身份校验统一，以及上一版本发布记录。
- 目标 tag、npm 精确版本与 GitHub Release 均未占用；npm `latest=0.3.5`。
- GitHub 账户具备 push/admin/repo/workflow 权限；两个 workflow active，`npm-production` 允许 `v*` tag。

## 发布内容

- Git Skill 组合不再依赖宿主分支命名或快照校验实现；公开 Skills 的资源闭包可在隔离消费者中验证。
- `submit-pr` 与 `worktree-rebase-merge` 支持显式 Git 字面分支名并保留保护分支与冲突 guard。
- `review-pr` 在并行采集结束后复验 PR 身份，阻止身份不一致的快照保存或指纹生成。

## 真实发布证据

- 本地通过 `git diff --check`、6 个 Skills/发现入口校验、9 项发现校验、Python compileall、3 个 Agents
  校验、6 项 Agent installer、19 项 git-commit、9 项 worktree、99 项 review-pr、10 项 review、3 项
  Skill portability、38 项 submit-pr 测试及 Shell 语法检查。
- `npm pack --dry-run --json`：6 个文件，候选 shasum `4e7f5f00ebeb71025c2e81c6c936e8212650a41f`，integrity
  `sha512-tIBncF+u/MqWRoZdEXptG8SjJhuqU/lH3e0eR0r+U5AwNJlMGzgRkYuUFw9AtcG5tdeI/4uZOH0xT84ujV5pdg==`。
- 发布提交：`3f47f97b1f89d0bf7d648cd3fd9db26d337a3bc2`；普通 CI
  [`34576706605`](https://github.com/tisfeng/skills/actions/runs/34576706605) 通过。
- annotated tag `v0.3.6`：tag object `50de543bbe3c69873150b2238f7d78aa40203008`，peeled commit 为发布提交；
  tag workflow [`34576845289`](https://github.com/tisfeng/skills/actions/runs/34576845289) 的验证、OIDC npm
  publish 与 GitHub Release 三个 job 均通过。
- npm registry：`@tisfeng/codex-agents@0.3.6` 已发布且 `latest=0.3.6`；6 个文件、shasum、integrity 与
  候选一致，附有 SLSA provenance 与 registry 签名。隔离安装的 `npm audit signatures --json` 返回
  `invalid=[]`、`missing=[]`。
- GitHub Release：[`v0.3.6`](https://github.com/tisfeng/skills/releases/tag/v0.3.6) 已发布，非 draft、非
  prerelease、无 assets；标题精确等于 tag，正文与 GitHub Release Notes API 生成的
  `v0.3.5...v0.3.6` Full Changelog 一致。
- 隔离安装：固定 `tisfeng/skills#v0.3.6` 安装 7 个 Skills，全部 lock ref 为 `v0.3.6`；精确 npm `0.3.6`
  安装 3 个 Agents，Agent lock revision 均为发布提交，重复 update 均返回 `Already current`。

## 完成条件

- [x] 发布提交已在远程 `main` 且普通 CI 通过。
- [x] `v0.3.6` tag、npm `0.3.6`、`latest` 和 GitHub Release 一致。
- [x] registry 摘要/签名与候选一致，固定 tag Skills 与精确 npm Agents 隔离安装通过。
- [x] 计划与 history 已回填真实证据并作为发布记录提交 push；公开 tag 保持在发布提交。
