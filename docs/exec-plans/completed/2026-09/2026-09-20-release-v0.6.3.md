# 发布 Skills v0.6.3

- 状态：completed
- 创建日期：2026-09-20
- 负责人：Codex
- 关联 Issue/PR：none

## 执行上下文

- **Agent Name:** `Codex`
- **Model:** `GPT-5`
- **Environment:** `macOS 27.0 / Xcode 27.0 (27A266a)`

## 背景

`v0.6.2` 之后，候选包含发布 workflow 复用同 SHA `main` 校验的性能优化、纯发布记录的独立
文档校验，以及 `git-commit` 双语标题语言校验修复。发布审查还发现并修复了 GitHub API 查询失败
时未按契约回退完整验证的问题。用户已授权发布 `0.6.3`。

## 目标与范围

- 目标结果：发布 `v0.6.3`，使发布提交、annotated tag、GitHub Release、固定 tag 隔离安装和发布记录可核验一致。
- 允许修改路径：本计划 active/completed 路径，以及 `docs/histories/2026-09/2026-09-20-release-v0.6.3.md`。
- 同任务 history：`docs/histories/2026-09/2026-09-20-release-v0.6.3.md`
- 用户限制：不新增手写 Release 正文；不移动或覆盖公开 tag；不发布 npm；固定 tag 验收仅使用临时目录。
- 非目标：不修改已冻结候选功能；若发布门禁失败，停止在对应阶段并按恢复规则处理。
- 验收标准：发布提交普通 CI 通过；`v0.6.3` tag peeled SHA、GitHub Release 标题和自动生成正文一致；固定 tag 安装只包含公开 Skills；发布记录已推送。

## 工作计划

1. 冻结 `v0.6.2..HEAD` 候选，完成完整 review 和验证矩阵。
2. 创建发布提交，复验远端基线后 push `main`，等待该 SHA 的普通 CI 成功。
3. 再次核验 `v0.6.3` 未占用，创建并 push annotated tag。
4. 等待 tag workflow 创建 GitHub Release，核验 tag、Release 和 workflow 证据。
5. 从固定 tag 做临时消费者安装验收，回填 history、归档计划并 push 发布记录。

## 风险与决策

- 当前 `main` 比 `origin/main` 超前 1 个提交；发布提交必须建立在当前 `e8febb568ec86d88fbd6c82beade3c3c545ccdc9` 上，并在 push 前复验远端没有漂移。
- 候选修复提交校验缺口且没有声明破坏性变化，按补丁版本 `v0.6.3` 发布。
- 普通 CI 失败时不创建 tag；公开 tag 校验失败且需要修改代码时使用下一版本；Release 创建失败只恢复 Release 阶段。
- 新增的 CI 复用逻辑必须以本次实际发布证明；静态 workflow 检查不能替代远端运行证据。

## 进度

- [x] 完成初始只读预检并确认目标版本、候选端点、远端基线和占用状态。
- [x] 完成候选 review 和完整验证。
- [x] 创建发布提交并推送 `main`，等待普通 CI。
- [x] 创建 annotated tag，等待并验收 Release workflow。
- [x] 完成固定 tag 安装验收、回填 history、归档计划并推送发布记录。

## 验证

- 初始快照：`v0.6.2..e8febb568ec86d88fbd6c82beade3c3c545ccdc9`；工作树和索引干净。
- 初始远端 `main`：`6578463510c06cdfa2cb92a055e73527a24122a9`；本地领先 1 个提交。
- `v0.6.3` 远端 tag 与 GitHub Release 均未占用；GitHub 认证可用。
- 已通过 `python3.12 scripts/validate-skills.py`、`python3.12 -m compileall -q scripts skills`、
  workflow YAML 解析、Shell 语法检查和 `git diff --check`。
- 完整验证矩阵共 116 项测试通过；发布 workflow 的成功查询与 API 失败 fallback 模拟均通过。
- Review 发现 API 查询失败会被 `set -e` 提前终止；修复为显式回退完整 tag 验证，并改用真实的
  `updated_at` 排序。复验未发现剩余 finding。
- 发布提交 `a49a909bdf1b19db281f212e32cd1f3af0b54c30` 已推送；普通 CI
  [`35510175415`](https://github.com/tisfeng/skills/actions/runs/35510175415) 和文档检查
  [`35510175418`](https://github.com/tisfeng/skills/actions/runs/35510175418) 成功。
- annotated tag `v0.6.3` 的 tag object 为 `b3f23c01fea0bfd7457301e1b524e0d0cbd2a54c`，peeled commit 为发布提交。
- Release workflow [`35510236250`](https://github.com/tisfeng/skills/actions/runs/35510236250) 成功，明确复用
  普通 CI `35510175415` 并跳过重复完整验证。
- GitHub Release [`v0.6.3`](https://github.com/tisfeng/skills/releases/tag/v0.6.3) 非 draft、非 prerelease，
  标题精确为 tag，正文为 GitHub 自动生成的 `v0.6.2...v0.6.3` Full Changelog。
- 固定 tag 临时消费者安装 6 个公开 Skills；所有 lock ref 为 `v0.6.3`，逐文件内容和独立重算的
  目录 SHA-256 均与 tag 源码及 `skills-lock.json` 一致，内部 `release` Skill 未安装。

## 完成条件

- [x] 发布提交已在远程 `main` 且普通 CI 通过。
- [x] `v0.6.3` tag peeled SHA、GitHub Release 标题与默认正文一致。
- [x] 固定 tag Skills 隔离安装通过，且只包含公开 Skills。
- [x] 计划与 history 已回填真实证据并推送；公开 tag 保持在发布提交。
