# 发布 Skills v0.3.4

- 状态：completed
- 创建日期：2026-09-10
- 负责人：Codex
- 关联 Issue/PR：none

## 任务摘要

将 `v0.3.3` 之后的 Git/PR 工作流可靠性、效率与本仓库 Skills 发现改进发布为
`@tisfeng/codex-agents@0.3.4`，使 npm package、Git tag、GitHub Release 和发布提交保持可核验一致。

## 授权与范围

- 意图模式：implementation
- 交付授权：release，包含版本与日志修改、本地提交、push `main`、创建并 push annotated `v0.3.4` tag、
  GitHub Actions Trusted Publishing、GitHub Release、发布后验收和发布记录 push。
- 仓库允许路径：`package.json`、`docs/release/changelog/0.3.4.md`、本计划的 active/completed 路径和
  `docs/histories/2026-09/2026-09-10-publish-v0.3.4.md`。
- 外部允许目标：`origin/main`、`refs/tags/v0.3.4`、`@tisfeng/codex-agents@0.3.4`、对应 GitHub Actions
  与 GitHub Release。
- 持续限制：不修改用户真实 Codex 全局配置；安装验收只使用临时目录；不移动已公开 tag。

## 初始 Git 与发布预检

- `initial_head`：`a421d5db1b20b98fbc05dee010166e131f65431d`
- 分支：`main`
- 初始 staged、unstaged、untracked、冲突：none
- 远程 `main`：`4833d34612aef0be534ddf8cb36dfe55cd996795`；本地仅领先 6 个后续提交，无分叉。
- 发布范围：`v0.3.3..HEAD` 共 7 个提交；其中首项为已发布版本的归档记录，6 项为本次面向用户的变更。
- 版本和占用：当前 package 为 `0.3.3`，目标 `v0.3.4` 的远程 tag、npm 精确版本和 GitHub Release 均未占用；
  npm `latest=0.3.3`。
- 认证与自动化：GitHub 账户具备 repo/workflow 权限；普通和发布 workflows 为 active，`npm-production`
  environment 的 tag policy 为 `v*`。本次 OIDC publish 由成功的 tag workflow 证明。

## 发布内容

- Git 提交和 worktree 集成减少重复读取，同时保留精确暂存、内容冻结和漂移门禁。
- PR 提交按用户语言起草，PR 审查使用完整分页快照、增量刷新与手动回退 head 复验。
- 本仓库通过相对链接发现当前 checkout 的六个公开 Skills。

## 真实发布证据

- 发布前本地验证通过：6 个公开 Skills、3 个 Agents、9 项 Skills 发现校验、Python compileall、6 项
  Agent installer、19 项 git-commit、9 项 worktree、35 项 review-pr、32 项 submit-pr 测试、Shell 语法与
  `git diff --check`。
- `npm pack --dry-run --json`：6 个文件，候选 shasum
  `e1f005150900f0946c666efd71379e136698cb65`，integrity
  `sha512-TStZx7DkcStHoJ5wNZkCnYNbw9apEZGHVmexct2c35veK9X2stB140hkwifSIJnC5U8EzQscBqVQQwixBAN/zQ==`。
- 发布提交：`e79154ef1fc076c8c7b6b6a06b46388596d485d2`；普通 CI
  [`34495714684`](https://github.com/tisfeng/skills/actions/runs/34495714684) 通过。
- annotated tag `v0.3.4`：tag object `02fe3a1ca9736f72aed005d0341506478d0051d8`，peeled commit
  为发布提交；tag workflow [`34495862401`](https://github.com/tisfeng/skills/actions/runs/34495862401)
  的验证、OIDC npm publish 与 GitHub Release 三个 job 均通过。
- npm registry：`@tisfeng/codex-agents@0.3.4` 已发布且 `latest=0.3.4`；6 个文件、shasum、integrity
  与候选完全一致，附有 SLSA provenance 与 registry 签名。隔离安装后
  `npm audit signatures --json` 返回 `invalid=[]`、`missing=[]`。
- GitHub Release：[`Skills v0.3.4`](https://github.com/tisfeng/skills/releases/tag/v0.3.4) 已发布，非 draft、
  非 prerelease，正文与 `docs/release/changelog/0.3.4.md` 一致。
- 隔离安装：固定 `tisfeng/skills#v0.3.4` 安装 7 个可发现的 Skills（包括项目内 `release`），
  `skills-lock.json` 均记录 `v0.3.4`；精确 npm `0.3.4` 安装 3 个 Agents，Agent lock 的 revision
  均为发布提交，重复 update 全部返回 `Already current`。

## 完成条件

- [x] 发布提交已在远程 `main` 且普通 CI 通过。
- [x] `v0.3.4` tag、npm `0.3.4`、`latest` 和 GitHub Release 一致。
- [x] 注册表摘要/签名与候选一致，固定 tag Skills 与精确 npm Agents 隔离安装通过。
- [x] 计划与 history 回填真实证据并作为发布记录提交 push；公开 tag 保持在发布提交。
