# 发布 Skills v0.3.7

- 状态：completed
- 创建日期：2026-09-11
- 负责人：Codex
- 关联 Issue/PR：none

## 任务摘要

发布 `v0.3.7`，使发布提交、`v0.3.7` tag、GitHub Release 与发布记录保持可核验一致。

## 授权与范围

- 用户明确发布 `0.3.7`，包含提交、push `main`、annotated tag、GitHub Actions 的 Release 创建、
  GitHub Release、发布后验收和发布记录 push。
- 仓库修改路径：本计划 active/completed 路径与
  `docs/histories/2026-09/2026-09-11-publish-v0.3.7.md`。
- 外部目标：`origin/main`、`refs/tags/v0.3.7`、对应 GitHub Actions 和 GitHub Release。
- 持续限制：不修改全局配置；隔离验收仅使用临时目录；不移动或覆盖公开 tag；不手写发布日志；
  不发布或 deprecate npm 包。

## 初始发布预检

- 初始 HEAD、`main`、`origin/main`：`142cf147f5f518c56358bc423bb29327d87fc2fd`；`origin/main`
  为 `9ada34da01d6ab4cf7380c9071da3f418341d9bc`；staged、unstaged、untracked、冲突：none。
- 发布范围：`v0.3.6..HEAD` 共 6 个提交、33 个路径，包含子代理与 npm 安装器移除、治理文档收敛，
  以及上一条发布记录的归档。
- 版本与占用：仓库不再有版本清单文件，版本身份由 tag 表达；本地/远程 `v0.3.7` 与 GitHub Release
  均未占用。
- 认证与自动化：GitHub 账户具备 push/admin/repo/workflow 权限；`Validate Skills` 与 `Release`
  workflow active；最近一次 `origin/main` 普通 CI 已通过。

## 发布内容

- 仓库移除 Codex 子代理、npm 安装器与 `package.json`，只发布平台无关的 Skills；发布流程改为统一
  校验通过后创建 GitHub Release。
- 治理、设计、发布文档与两份 README 收敛为单资产结构，不再描述已移除的子代理、安装器或 npm 包。
- 公开 Skills 的源码与行为未改动，本版本不新增、移除或修改任何公开 Skill。

## 真实发布证据

- 候选验证：`git diff --check`、6 个 Skills 与发现入口校验、Python compileall、19 项 git-commit、
  9 项 worktree-rebase-merge、99 项 review-pr、10 项 review、38 项 submit-pr、3 项隔离消费者测试
  与 Shell 语法检查全部通过。
- 发布提交：`3e8ceef4ac667a4102943b386d590ccd243b6575`；普通 CI
  [`34601666230`](https://github.com/tisfeng/skills/actions/runs/34601666230) 通过。
- annotated tag `v0.3.7`：tag object `82e36a557491ef1be822df24bb12beba5eef3e05`，peeled commit 为
  发布提交；tag workflow [`34601778678`](https://github.com/tisfeng/skills/actions/runs/34601778678)
  的 validate 与 Create GitHub Release 两个 job 均通过，tag push 触发的普通 CI
  [`34601778136`](https://github.com/tisfeng/skills/actions/runs/34601778136) 也通过。
- GitHub Release：[`v0.3.7`](https://github.com/tisfeng/skills/releases/tag/v0.3.7) 已发布，非 draft、
  非 prerelease、无 assets；标题精确等于 tag，正文与 GitHub Release Notes API 生成的
  `v0.3.6...v0.3.7` Full Changelog 一致。
- 隔离安装：固定 `tisfeng/skills#v0.3.7` 在临时目录安装 7 个 Skills，全部 lock ref 为 `v0.3.7`。

## 完成条件

- [x] 发布提交已在远程 `main` 且普通 CI 通过。
- [x] `v0.3.7` tag peeled SHA、GitHub Release 标题与默认正文一致。
- [x] 固定 tag Skills 隔离安装通过。
- [x] 计划与 history 已回填真实证据并作为发布记录提交 push，公开 tag 保持在发布提交。
