# 发布 Skills v0.3.3

- 状态：completed
- 创建日期：2026-09-10
- 负责人：Codex
- 关联 Issue/PR：none

## 任务摘要

将 `v0.3.2` 之后已在本地 `main` 的 Agent 工作流改进作为
`@tisfeng/codex-agents@0.3.3` 发布，并保持 npm package、Git tag、GitHub Release 与发布提交一致。

## 授权与范围

- 意图模式：implementation
- 交付授权：release，包含版本与日志修改、本地提交、push `main`、创建并 push `v0.3.3` annotated tag、
  GitHub Actions Trusted Publishing、GitHub Release、发布后验收和发布记录 push。
- 仓库允许路径：`package.json`、`docs/release/changelog/0.3.3.md`、本计划的 active/completed 路径和
  `docs/histories/2026-09/2026-09-10-publish-v0.3.3.md`。
- 外部允许目标：`origin/main`、`refs/tags/v0.3.3`、`@tisfeng/codex-agents@0.3.3`、对应 GitHub Actions
  与 GitHub Release。
- 持续限制：不修改用户真实 Codex 全局配置；安装验收只使用临时目录；不移动已公开 tag。

## 初始 Git 与发布预检

- `initial_head`：`7fd4cea6383fc504f5bf6d1eed845b553073a834`
- 分支：`main`
- 初始 staged、unstaged、untracked、冲突：none
- 远程 `main`：`31fedaa117008f575987100e9f0475c986906d5f`；本地仅领先 5 个提交，无分叉。
- 发布范围：`v0.3.2..HEAD`，6 个提交、30 个文件；独立 reviewer 未发现阻塞发布的确定缺陷。
- 版本、远程 tag、npm 精确版本和 GitHub Release：当前 `0.3.2`；目标 `v0.3.3` 均明确未占用；npm
  `latest=0.3.2`。
- 认证与自动化：GitHub 账号拥有 admin 权限；`npm-production` 环境和两条 workflow 均为 active。
- 独立规划：planner 已核对执行顺序、停止条件和恢复路径，结论为可继续发布。

## 发布内容

- 移除 `git-delivery`，由当前模型直接执行已授权的 Git 操作，并记录旧安装的迁移要求。
- 统一提交、PR 准备与 worktree 集成的预览、暂存和状态复验边界。
- 复用同一快照上的有效规划、验证与仓库查询证据，减少不必要的工作流往返。

## 真实发布证据

- 发布候选本地验证通过：6 个公开 Skills、3 个 Agents、Python compileall、6 项 Agent installer、
  19 项 git-commit、27 项 review-pr、26 项 submit-pr 测试、Shell 语法与 `git diff --check`。
- `npm pack --dry-run --json`：6 个文件，候选 shasum
  `dd4a7b9c1767443f6adeb15b06b889db93df769f`，integrity
  `sha512-JieOnVvl06qbH1tBGypC5w1no/0Wv1awUpuUmUjbCNnaZbHpnLgXJlfd9br6eXeC7BBrM7OQqN4tp1xUsOBmIQ==`。
- 发布提交：`41555fa9a5090dd172870ba392b2dc231c767c9b`；普通 CI
  [`34429060147`](https://github.com/tisfeng/skills/actions/runs/34429060147) 通过。
- annotated tag `v0.3.3`：tag object `19a2f7b447cdc1b4570164ab768e4504bb616a8d`，peeled commit
  为发布提交；tag workflow [`34429197205`](https://github.com/tisfeng/skills/actions/runs/34429197205)
  的验证、OIDC npm publish 与 GitHub Release 三个 job 均通过。
- npm registry：`@tisfeng/codex-agents@0.3.3` 已发布且 `latest=0.3.3`；6 个文件、shasum、integrity
  与候选完全一致，附有 SLSA provenance 与 registry 签名。隔离安装后
  `npm audit signatures --json` 返回 `invalid=[]`、`missing=[]`。
- GitHub Release：[`Skills v0.3.3`](https://github.com/tisfeng/skills/releases/tag/v0.3.3) 已发布，非 draft、
  非 prerelease，正文与 `docs/release/changelog/0.3.3.md` 一致。
- 隔离安装：固定 `tisfeng/skills#v0.3.3` 安装 7 个可发现的 Skills（包括项目内 `release`），
  `skills-lock.json` 均记录 `v0.3.3`；精确 npm `0.3.3` 安装 3 个 Agents，Agent lock 的 revision
  均为发布提交，重复 update 全部返回 `Already current`。

## 完成条件

- [x] 发布提交已在远程 `main` 且普通 CI 通过。
- [x] `v0.3.3` tag、npm `0.3.3`、`latest` 和 GitHub Release 一致。
- [x] 注册表摘要/签名与候选一致，固定 tag Skills 与精确 npm Agents 隔离安装通过。
- [x] 计划与 history 回填真实证据并作为发布记录提交 push；公开 tag 保持在发布提交。
