# 发布 Skills v0.3.5

- 状态：completed
- 创建日期：2026-09-11
- 负责人：Codex
- 关联 Issue/PR：none

## 任务摘要

发布 `@tisfeng/codex-agents@0.3.5`，使发布提交、`v0.3.5` tag、npm、GitHub Release 与发布记录保持可核验一致。

## 授权与范围

- 用户明确发布 `0.3.5`；授权包含版本修改、提交、push `main`、annotated tag、GitHub Actions Trusted
  Publishing、GitHub Release、发布后验收和发布记录 push。
- 仓库修改路径：`package.json`、本计划 active/completed 路径与
  `docs/histories/2026-09/2026-09-11-publish-v0.3.5.md`。
- 外部目标：`origin/main`、`refs/tags/v0.3.5`、`@tisfeng/codex-agents@0.3.5`、对应 GitHub Actions 和
  GitHub Release。
- 持续限制：未修改全局配置；隔离验收仅使用临时目录；未移动或覆盖公开 tag；未手写发布日志。

## 初始发布预检

- 初始 HEAD：`6b851b7baedf79376ca93a3b5e7dabc0d281caea`；分支 `main`；staged、unstaged、untracked、冲突：none。
- 远程 main：`b71b1f025b3389eb57ea27dfe5fea843eab12cd6`，为初始 HEAD 祖先；本地领先 3 个 review 改进提交。
- 发布范围：`v0.3.4..HEAD` 共 6 个提交、37 个路径，包含 GitHub 默认 Release Notes 政策与 review/review-pr
  的证据收集、刷新和目标核验改进。
- 目标 `v0.3.5`、npm 精确 `0.3.5` 与 GitHub Release 在发布前均未占用，npm `latest=0.3.4`。
- GitHub 账户具备 repo/workflow 权限；普通和发布 workflows 为 active，`npm-production` 仅允许 `v*` tag。

## 发布内容

- `review` 新增本地快照采集、无损 patch 分页及紧凑复验，减少重复读取并保持只读边界。
- `review-pr` 补齐冻结 head/base 的准备回执、证据分页与线程差量刷新，并核对关联 Issue 的功能目标、实现和测试证据。
- 发布工作流和 release Skill 改为标题精确使用 tag、正文由 GitHub 默认生成；新版本不再创建手写发布日志。

## 真实发布证据

- 本地通过 `git diff --check`、6 个 Skills/发现入口校验、9 项发现校验单测、Python compileall、3 个
  Agents 校验、6 项 Agent installer、19 项 git-commit、9 项 worktree、89 项 review-pr、10 项 review、
  32 项 submit-pr 测试及 Shell 语法检查。
- `npm pack --dry-run --json`：6 个文件，候选 shasum `e37e5cd7af06477f8ae58c14d4b38a44cc560ba4`，integrity
  `sha512-wPbX9uQitUinG2x4jm4MekrMJm2hPKyCQ57mxCnKKVCrHjTYX588/RnHwM4fOq9DcHJpun4vMu7ZGDcQyZgpVQ==`。
- 发布提交：`a04dd44c581bda55c4da6d6a842b4d6fdc110ff1`；普通 CI
  [`34516384278`](https://github.com/tisfeng/skills/actions/runs/34516384278) 通过。
- annotated tag `v0.3.5`：tag object `ae69302a29a3fac9bacee6ad1972ce3f6b89e6e0`，peeled commit 为发布提交；
  tag workflow [`34516550137`](https://github.com/tisfeng/skills/actions/runs/34516550137) 的验证、OIDC npm
  publish 与 GitHub Release 三个 job 均通过。
- npm registry：`@tisfeng/codex-agents@0.3.5` 已发布且 `latest=0.3.5`；6 个文件、shasum、integrity 与
  候选完全一致，附有 SLSA provenance 与 registry 签名。隔离安装后的 `npm audit signatures --json` 返回
  `invalid=[]`、`missing=[]`。
- GitHub Release：[`v0.3.5`](https://github.com/tisfeng/skills/releases/tag/v0.3.5) 已发布，非 draft、非
  prerelease、无 assets；标题精确等于 tag，正文与 GitHub Release Notes API 生成的
  `v0.3.4...v0.3.5` Full Changelog 一致。
- 隔离安装：固定 `tisfeng/skills#v0.3.5` 安装 7 个 Skills（包含项目内 `release`），全部 lock ref 为
  `v0.3.5`；精确 npm `0.3.5` 安装 3 个 Agents，Agent lock revision 均为发布提交，重复 update 全部返回
  `Already current`。

## 完成条件

- [x] 发布提交已在远程 `main` 且普通 CI 通过。
- [x] `v0.3.5` tag、npm `0.3.5`、`latest` 和 GitHub Release 一致。
- [x] registry 摘要/签名与候选一致，固定 tag Skills 与精确 npm Agents 隔离安装通过。
- [x] 计划与 history 已回填真实证据并作为发布记录提交 push；公开 tag 保持在发布提交。
