# 发布 codex-agents v0.2.0

- 日期：2026-09-08
- 状态：completed
- 关联计划：none

## 目标

将 `@tisfeng/codex-agents` 的 npm package 版本与 `v0.2.0` Git tag 对齐，并发布公开安装命令。

## 实际变更

- 将 package 版本设为 `0.2.0`。
- 发布前验证 npm organization 所有权、包名可用性、打包内容和测试。

## 验证

记录 npm 发布结果、Git tag 以及从 GitHub tag 运行的安装器验证。

## 交付

发布公开 npm package，推送 `main` 和 `v0.2.0` tag；不修改用户全局 Codex agent 配置。
