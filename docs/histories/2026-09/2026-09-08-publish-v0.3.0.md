# 发布 Skills v0.3.0

- 日期：2026-09-08
- 状态：completed
- 关联计划：[`docs/exec-plans/completed/2026-09-08-release-v0.3.0.md`](../../exec-plans/completed/2026-09-08-release-v0.3.0.md)

## 目标

将 `v0.2.0` 以来的 Skills、Codex 子代理和 README 改进作为 `v0.3.0` 统一发布，
并保持 npm package、Git tag 与 GitHub Release 可核验地对齐。

## 发布内容

- 恢复并完善 implementation 自动本地提交、精确暂存与 `git-delivery` prepare/apply 门禁。
- 将 README 改为中文默认页，合并项目/全局安装说明，并优化中英文导航。
- 为 `code-simplifier` 增加通用 Electron/TypeScript/React 专项规则。

## 预检

- GitHub 和 npm 认证已就绪。
- npm `latest` 为 `0.2.0`，`0.3.0` 未占用。
- 远程无 `v0.3.0` tag；当前工作树干净且可与远程 `main` 线性对齐。

## 发布前验证

- `scripts/validate-skills.py` 验证 6 个 Skills，`scripts/validate-agents.py` 验证 4 个子代理。
- `git-commit`、`review-pr` 和 `submit-pr` 分别通过 19、27 和 21 项单元测试。
- 子代理安装器通过 4 项 Node 测试。
- Python 编译、Shell 语法与 `git diff --check` 通过。
- 隔离 npm cache 下的 `npm pack --dry-run --json` 通过；`0.3.0` tarball 仅包含
  LICENSE、中英文 README、bin 入口、`package.json` 与 `src/cli.mjs`。

## 交付

- 发布提交：`ccc74f119f61d672cfd0cb57c07a259b7bc78614`。
- `main` CI：[Validate Skills 34203714717](https://github.com/tisfeng/skills/actions/runs/34203714717)，通过。
- `v0.3.0` CI：[Validate Skills 34203868452](https://github.com/tisfeng/skills/actions/runs/34203868452)，通过。
- annotated tag `v0.3.0` 指向发布提交。
- npm `@tisfeng/codex-agents@0.3.0` 已公开发布，`latest=0.3.0`，tarball shasum 为
  `7acbdea6a874703a96afccc6d2c2194f2efe98ac`。
- [GitHub Release `Skills v0.3.0`](https://github.com/tisfeng/skills/releases/tag/v0.3.0) 已发布。

## 发布后验证

- 在隔离的临时项目中从 `tisfeng/skills#v0.3.0` 安装 6 个 Skills 和 4 个子代理。
- Skills lock 的 6 个 ref 均为 `v0.3.0`；agent lock 的 4 个 revision 均为发布提交。
- 已安装内容与 tag 源码一致；Skills update 重新获取后内容不变，子代理 update 全部为
  `Already current`。
- npm publish 首次尝试因 sandbox 无法打开官方二次认证页而在上传前退出；重验版本未占用后，
  使用相同内容完成官方网页认证并发布。
- GitHub Actions 报告 Node 20 action runtime 弃用提醒，本次两个工作流均成功；该提醒可作为后续
  CI 维护处理，不影响本次发布结果。

本次未修改用户真实 Codex 全局配置，未执行 rebase 或 merge。
