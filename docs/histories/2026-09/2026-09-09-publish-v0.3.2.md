# 发布 Skills v0.3.2

- 日期：2026-09-09
- 状态：completed
- 关联计划：[`docs/exec-plans/completed/2026-09-09-release-v0.3.2.md`](../../exec-plans/completed/2026-09-09-release-v0.3.2.md)

## 目标

发布 `@tisfeng/codex-agents@0.3.2`，并让 npm 包、Git tag、GitHub Release 与发布提交保持可核验一致。

## 发布内容

- `worktree-rebase-merge` 复用统一的空索引显式交付暂存决策。
- `git-delivery` 明确区分已有索引、指定路径、完整工作树和自动精确暂存，并复验冻结范围。
- 中英文安装文档、源码目录链接和 npm repository 元数据获得改进。

## 验证与发布

- 发布前本地校验通过：6 个公开 Skills、4 个 Agents、compileall、Agent installer、git-commit、
  worktree、review-pr、submit-pr 测试、Shell 语法、`git diff --check` 与 `npm pack --dry-run --json`。
- 候选和 registry 均为 6 个 npm 文件，shasum
  `b40a92ae6b2bd72bfa8b6b5d9a501ae0fe55cb72`，integrity
  `sha512-fGlWAtIWEmdvc0R4+RyypPquKHAmXxYv3pOvSYng+VtrzXtr+Npsvsgp6p6WfWudvMNBC3LppDB7RfLdvbxBTw==`。
- 发布提交 `826b0a868f5802da8a4f2b90c8ac92e6cb2577b2` 的普通 CI
  [`34333131646`](https://github.com/tisfeng/skills/actions/runs/34333131646) 通过；annotated tag
  `v0.3.2` 的 peeled commit 与该提交一致。
- tag workflow [`34333263811`](https://github.com/tisfeng/skills/actions/runs/34333263811) 的 release
  validate、OIDC npm publish 与 GitHub Release 均通过。npm `0.3.2` 和 `latest` 一致，registry
  签名审计的 `invalid`、`missing` 均为空。
- GitHub Release [`Skills v0.3.2`](https://github.com/tisfeng/skills/releases/tag/v0.3.2) 已发布，正文与
  changelog 一致。隔离目录的 fixed-tag Skills lock 记录 `v0.3.2`，精确 npm Agents lock 记录该 tag 和
  发布提交；Agents 的重复 update 均返回 `Already current`。

## 交付

- 公开 tag 固定在发布提交；本记录将通过独立文档提交推送到 `origin/main`，不移动 tag。
