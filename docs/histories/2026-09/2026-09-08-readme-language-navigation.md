# 优化 README 语言导航

- 日期：2026-09-08
- 状态：completed

## 目标

改善中英文 README 顶部的语言入口展示，使当前语言和可切换语言更容易辨识。

## 实际变更

- 居中显示 `Skills` 标题和语言导航。
- 当前语言使用粗体，可切换语言保留链接。
- 使用较轻的中点分隔，并将双向跳转保持为相对路径。

## 验证

- 检查中英文页头结构和双向链接。
- 运行 `git diff --check`。

## 交付

本任务通过自动本地提交门禁交付；不执行 push、pull、rebase、merge 或发布。
