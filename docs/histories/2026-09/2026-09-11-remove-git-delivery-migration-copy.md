# 移除 git-delivery 迁移文案

- 删除根 README 中面向用户的旧版 `git-delivery` 清理和选择性更新说明。
- 同步删除英文 README 与 v0.3.3 changelog 中的同类迁移操作提示。
- 保留 `git-delivery` 的版本变更历史，以及安装器通用的更新和本地修改保护说明。
- 验证：`git diff --check`、`npm run test:agents`。
