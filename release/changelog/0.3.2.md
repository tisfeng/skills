# v0.3.2

- 发布日期：2026-09-09

## 修复

- 修复 `worktree-rebase-merge` 在空索引下无法继续显式交付的问题，并与 `git-commit` 复用一致的暂存决策。
- 让 `git-delivery` 在已有索引、指定路径、完整工作树和自动精确暂存之间明确区分，并在写入前后复验冻结范围。

## 变更

- 改进中英文安装文档和源码目录链接，并规范 npm `repository.url` 元数据。
